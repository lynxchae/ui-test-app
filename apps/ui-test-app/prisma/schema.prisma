datasource db {
  provider = "mysql"
  url      = env("DB_URL")
}

generator client {
  provider = "prisma-client-js"
}

model MainTestInformation {
  createdAt DateTime @default(now())
  host      String?
  id        String   @id @default(cuid())
  updatedAt DateTime @updatedAt
}
